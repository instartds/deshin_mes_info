<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mailServiceImpl">

    <!-- 경비관리 계좌 update -->
    <update id="mailServiceImpl.updateRecv" parameterType="Map">
    /* mailServiceImpl.updateRecv */   
    UPDATE  TB_MAIL_SEND_CANDIDATE
       SET  APPR_RETURN = '1'
     WHERE  JOB_ID = #{JOB_ID} 
    </update>   

</mapper>
