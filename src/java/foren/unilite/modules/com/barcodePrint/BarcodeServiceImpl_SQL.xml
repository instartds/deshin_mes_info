<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="barCodeServiceImpl">
	
<select id="barCodeServiceImpl.selectRefCode" parameterType="Map" resultType="rMap">	
	
	
	
</select>
	


<select id="barCodeServiceImpl.barCodeDataSelect" parameterType="Map" resultType="rMap">
	SELECT TOP 1 ITEM_CODE,ITEM_NAME,SPEC, convert(varchar, getdate(), 112) AS TODAY FROM BPR100T 
</select>

</mapper>